```
1. ctrl+shift+p  然后输入 >c/c++: edit configurations  生成c_cpp_properties.json

2. 如果没有launch.json文件，点击左边菜单栏的run and debug，会有一个 create a launch.json file的链接，
   选择C++(GDB/LLDB)。

3. ctrl+shift+p, 键入Configure Task，

```